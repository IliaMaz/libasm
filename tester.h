#pragma once

#include <stddef.h>
#include <stdio.h>
#include <string.h>
#include <assert.h>

// Functions to test
extern size_t ft_strlen(char *);

// Macros for tests
#define print_size_t(x) printf("%zu\n", x);
